% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_growth_rate.R
\name{calculate_growth_rate_power}
\alias{calculate_growth_rate_power}
\title{Calculate Growth Rate by Township (Power Function)}
\usage{
calculate_growth_rate_power(
  township = "T13R5",
  hw_volume,
  sw_volume,
  base_vol = 6,
  maxvol = 38
)
}
\arguments{
\item{township}{Character. The stand's township code (e.g. "T10R9", "Davis").}

\item{hw_volume}{Numeric. Hardwood volume in cords/acre.}

\item{sw_volume}{Numeric. Softwood volume in cords/acre.}

\item{base_vol}{Numeric. Volume (cords/acre) below which no decline applies. Default: 6.}

\item{maxvol}{Numeric. Biological maximum volume for decline calculation. Default: 38.}
}
\value{
Numeric. The predicted annual growth rate (cords/acre/yr), constrained between
the case-specific minimum floor and the weighted start rate.
}
\description{
`calculate_growth_rate_power()` computes a non-linear, power-law declining annual growth
rate for a stand, using its township code to select case-specific parameters.
The curve declines from a user-specified base volume up to a biological maximum.
}
\details{
Internally, each township is mapped to one of four "cases" (SP, RY, AE, AW),
each with its own softwood start rate, hardwood start rate, exponent \eqn{p}
and minimum floor rate. The function:
\enumerate{
  \item Splits total volume into HW and SW components.
  \item Computes the biological max via \code{\link{calculate_max_volume}}.
  \item Normalizes a "decline factor" from \code{base_vol} to \code{maxvol}.
  \item Applies a power-law:
  \deqn{rate = start\_rate - (start\_rate - min\_rate) \times decline\_factor^p}
  enforcing \eqn{rate \ge min\_rate}.
}
}
\examples{
# pure softwood in RY township:
calculate_growth_rate_power("Davis", hw_volume = 5, sw_volume = 25)

# mixed stand in SP township, below base_vol (no decline):
calculate_growth_rate_power("T10R15", hw_volume = 1, sw_volume = 4)

# at or above maxvol -> floor applies:
calculate_growth_rate_power("Upton", hw_volume = 30, sw_volume = 10)

}
\seealso{
\code{\link{calculate_max_volume}}, \code{\link{assign_spa_unit}}

Other Growth and Yield Functions: 
\code{\link{calculate_growth_rate}()},
\code{\link{calculate_max_volume}()}
}
\concept{Growth and Yield Functions}
