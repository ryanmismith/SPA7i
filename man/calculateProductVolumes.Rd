% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_product_volumes.R
\name{calculateProductVolumes}
\alias{calculateProductVolumes}
\title{Calculate product volumes based on AAC results and ratios.}
\usage{
calculateProductVolumes(aac_results, ratios_df, years = 20)
}
\arguments{
\item{aac_results}{A dataframe containing AAC results for multiple years.}

\item{ratios_df}{A dataframe containing the ratios for hardwood and softwood for each township and matrix.}

\item{years}{Integer, number of years to calculate product volumes for (default is 20).}
}
\value{
A dataframe containing the product harvest volume and standing volume for each township and matrix over the specified years.
}
\description{
This function calculates the product harvest and standing volumes for each township and matrix
based on the AAC results and hardwood/softwood ratios.
}
\examples{
# Simulated AAC results
aac_results <- data.frame(
  township = c("township1", "township1", "township2"),
  matrix = c("matrix1", "matrix2", "matrix1"),
  Year = c(1, 1, 1),
  HW_AAC = c(40, 40, 100),
  SW_AAC = c(25, 80, 100),
  HW_Volume = c(1000, 1200, 1500),
  SW_Volume = c(500, 800, 600)
)

# Simulated ratios dataframe
ratios_df <- data.frame(
  township = c("township1", "township1", "township2"),
  matrix = c("matrix1", "matrix2", "matrix1"),
  wood_type = c("HW", "SW", "HW"),
  HWratio = c(0.6, 0.7, 0.8),
  SWratio = c(0.4, 0.3, 0.2),
  acres = c(10, 20, 15)
)

}
\concept{Product Functions

# Example usage
product_volumes <- calculate_product_volumes(aac_results, ratios_df, years = 20)
print(product_volumes)}
